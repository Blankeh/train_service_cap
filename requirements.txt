ultralytics>=8.2.0
opencv-python>=4.9.0
numpy>=1.26.0
pyyaml>=6.0
httpx>=0.27.0
onnx>=1.16.0
onnxruntime>=1.18.0

# ── Pi4 export (install on training machine, not required on Pi4 itself) ──────
# ncnn export via pnnx (bundled with ultralytics ncnn export on recent versions)
# If pnnx is missing, ExportService falls back to ONNX automatically.
# ncnn                 # optional: pip install ncnn (or build from source for ARM)

# openvino             # optional: pip install openvino  (for OpenVINO IR export)
